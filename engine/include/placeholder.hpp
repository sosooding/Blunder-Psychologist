#pragma once

namespace blunder {

// Placeholder symbol so the Phase 0 CMake target compiles and links.
// Replaced in Phase 1 by the real UCI engine + feature-extraction interface.
int answer();

}  // namespace blunder
